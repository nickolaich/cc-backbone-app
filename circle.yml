dependencies:
  override:
    #- bundle install
    - sudo pip install awscli
    - npm install
    - ./node_modules/bower/bin/bower install

deployment:
  aws:
    branch: master
    commands:
      - gulp build
      - /bin/bash publish.sh